#include "main.h"

/**
 * _isupper - entry point
 *
 * Description: checks if letter is upppercase
 * @c: character to be checked
 *
 * Return: 1 if upper and 0 if else
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (1)
	else
		return (0);
}
